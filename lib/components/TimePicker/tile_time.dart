import 'package:flutter/material.dart';

class TileTime extends StatelessWidget {
  const TileTime({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
            color: Colors.black,
            child: const Text('Hello'),
          );
  }
}